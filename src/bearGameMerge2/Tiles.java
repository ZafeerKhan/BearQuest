/* Package: bearGameMerge
 * Class: GoodPlayer
 * Programmer: ICS4U
 * Date Created: January , 2016
 * Description: Organize all tiles of the game into the tiles class.
 */

package bearGameMerge2;

import java.awt.Graphics;
import java.awt.image.BufferedImage;

public class Tiles {
	
	//Create methods for each tile
	public static void drawDeer(Graphics g, int x, int y, BufferedImage deerPhoto) {
		g.drawImage(deerPhoto, x, y, null);
	}
	public static void drawGrave(Graphics g, int x, int y, BufferedImage gravePhoto) {
		g.drawImage(gravePhoto, x, y, null);
	}
	public static void drawGrass(Graphics g, int x, int y, BufferedImage grassPhoto) {
		g.drawImage(grassPhoto, x, y, null);
	}
	
	public static void drawTree(Graphics g, int x, int y, BufferedImage treePhoto) {
		g.drawImage(treePhoto, x, y, null);
	}
	
	public static void drawCave(Graphics g, int x, int y, BufferedImage cavePhoto) {
		g.drawImage(cavePhoto, x, y, null);
	}
	
	public static void drawBerryBush(Graphics g, int x, int y, BufferedImage berryBushPhoto) {
		g.drawImage(berryBushPhoto, x, y, null);
	}
	
	public static void drawBerryBushEmpty(Graphics g, int x, int y, BufferedImage berryBushEmptyPhoto) {
		g.drawImage(berryBushEmptyPhoto, x, y, null);
	}
	
	public static void drawWater(Graphics g, int x, int y, BufferedImage WaterPhoto) {
		g.drawImage(WaterPhoto, x, y, null);
	}
	
	public static void drawGWB(Graphics g, int x, int y, BufferedImage gWBPhoto) {
		g.drawImage(gWBPhoto, x, y, null);
	}
	
	public static void drawGWT(Graphics g, int x, int y, BufferedImage gWTPhoto) {
		g.drawImage(gWTPhoto, x, y, null);
	}
	
	public static void drawGWBR(Graphics g, int x, int y, BufferedImage grassWatBotRigPhoto) {
		g.drawImage(grassWatBotRigPhoto, x, y, null);
	}
	
	public static void drawGWTR (Graphics g, int x, int y, BufferedImage grassWatTopRightPhoto) {
		g.drawImage(grassWatTopRightPhoto, x, y, null);
	}
	
	public static void drawGWTL (Graphics g, int x, int y, BufferedImage grassWatTopLeftPhoto) {
		g.drawImage(grassWatTopLeftPhoto, x, y, null);
	}
	
	public static void drawGWBL (Graphics g, int x, int y, BufferedImage grassWatBotLeftPhoto) {
		g.drawImage(grassWatBotLeftPhoto, x, y, null);
	}
	public static void drawGWR(Graphics g, int x, int y, BufferedImage gWRPhoto) {
		g.drawImage(gWRPhoto, x, y, null);
	}
	
	public static void drawGWL(Graphics g, int x, int y, BufferedImage gWLPhoto) {
		g.drawImage(gWLPhoto, x, y, null);
	}
	public static void drawGWTRC(Graphics g, int x, int y, BufferedImage grassWatTopAndRigPhoto) {
		g.drawImage(grassWatTopAndRigPhoto, x, y, null);
	}
	
	public static void drawGWTLC(Graphics g, int x, int y, BufferedImage grassWatTopAndLefPhoto) {
		g.drawImage(grassWatTopAndLefPhoto, x, y, null);
	}
	
	public static void drawGWBRC(Graphics g, int x, int y, BufferedImage grassWatBotAndRigPhoto) {
		g.drawImage(grassWatBotAndRigPhoto, x, y, null);
	}
	
	public static void drawGWBLC(Graphics g, int x, int y, BufferedImage grassWatBotAndLefPhoto) {
		g.drawImage(grassWatBotAndLefPhoto, x, y, null);
	}
	
	public static void drawHunter(Graphics g, int x, int y, BufferedImage hunterPhoto) {
		g.drawImage(hunterPhoto, x, y, null);
	}
	
	public static void drawCave1(Graphics g, int x, int y, BufferedImage cave1Photo) {
		g.drawImage(cave1Photo, x, y, null);
	}
	
	public static void drawCave2(Graphics g, int x, int y, BufferedImage cave2Photo) {
		g.drawImage(cave2Photo, x, y, null);
	}
	
	public static void drawCave3(Graphics g, int x, int y, BufferedImage cave3Photo) {
		g.drawImage(cave3Photo, x, y, null);
	}
	
	public static void drawCave4(Graphics g, int x, int y, BufferedImage cave4Photo) {
		g.drawImage(cave4Photo, x, y, null);
	}
	
	public static void drawCave5(Graphics g, int x, int y, BufferedImage cave5Photo) {
		g.drawImage(cave5Photo, x, y, null);
	}
}
